{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "npm run test -- --watch",
    "dev": "NODE_ENV=development nodemon --exec tsx src/infra/express/server.ts",
    "build": "esbuild src/infra/express/server.ts --bundle --platform=node --format=esm --outfile=dist/server.js --external:mongoose --external:dotenv --external:express --external:cors --external:morgan",
    "start": "NODE_ENV=production node dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.2",
    "nodemon": "^3.1.14",
    "tsx": "^4.22.1",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
